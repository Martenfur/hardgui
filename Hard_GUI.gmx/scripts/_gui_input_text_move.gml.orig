///_gui_input_text_move(isnum)

var isnum=argument0;

curX=string_width(string_copy(str,1,curPos))

//Moving text.
if curX+textXadd>w-ARROW_TINY_SIZE*isnum
{textXadd=-curX+w-ARROW_TINY_SIZE*isnum}
else
{
  if curX+textXadd<0
  {textXadd=-curX}
}
if !active
{textXadd=0}
//Moving text.
